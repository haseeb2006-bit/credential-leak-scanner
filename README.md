# credential-leak-scanner
A Python-based DevSecOps security tool that scans Git repositories for leaked secrets like API keys, tokens, passwords, and private keys. It uses regex patterns and Shannon entropy analysis to detect hidden credentials before commits. Integrated with Git hooks and GitHub Actions to prevent insecure code from being pushed or merged.
